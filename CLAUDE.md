# CLAUDE.md — 博客精读 · blog-deepread

这是云端 routine 的执行说明。你是引擎本身（Opus），**自主完成、不等任何人确认**。当前工作目录已挂载本仓库。

定位：每期挑**一个**博客／写作者，把这个头脑**最精华的思想**提炼出来、讲透。目标是：**读完这一页，约等于把这个人读通了——他在想什么问题、给出了哪几个真正原创的答案、他怎么想问题、他哪里可能是错的——读者不必自己去爬完他十年几百篇文章。**

**这不是「博客简介」，不是「文章列表」，不是「安利」。是提纯一个头脑。**

一个长期写作者的价值，往往集中在两三个他反复回来、越挖越深的核心命题上，其余是这些命题在不同题材上的应用。你的活就是把那两三个命题拎出来、讲到读者能复述、能拿去用、能判断自己同不同意。

**但要讲得好看。** 这是本站和 `cs-papers-deepread` 的分野：论文精读可以严肃，博客精读不行——博客本来就是一个人用自己的声音写给你看的东西，把它压成结构化摘要就把它最好的部分弄丢了。**要有故事性、有趣、易读**：从一件具体的怪事开场，把想法讲成他发现它的过程，让他自己最好的那几句原话出来说话。

两件事同时成立，不冲突：**讲得好看**，且**讲完了读者就不必再回去读原文**。

---

## 0. 开工第一件事：读 `.preflight`

环境的 setup script 在容器启动时会探一次网络，把结论写进仓库根目录的 `.preflight`（gitignore 掉，不进版本库）。**动手前先 `cat .preflight`。**

**如果 `.preflight` 不存在**（说明这个环境没配 setup script，日志里会看到 `No setup script configured`）：**自己跑一次 `bash setup.sh`**，它会当场生成 `.preflight`。然后照下面的判定继续，并在本次运行的最后**在 PushNotification 里捎一句「环境未配 setup script」**，提醒 BigCat 去补——不然每次运行都要多花几秒重探。

- `egress: OK` → 照常做，本文件其余部分全部适用。
- `egress: BLOCKED` → **立刻停**。这是环境的网络策略，不是某个站反爬。**别再去试换 UA、试别的域名、试归档页**——探针里已经包含了一个中立对照站，它也失败了，再试是浪费。**更不许退回凭记忆写。** 正确动作：不建任何文件、不改任何文件、不跑 publish，只发一条 PushNotification 说明环境的 Network access 需要从 `Trusted` 改成 `Custom` 或 `Full`（`Trusted` 的白名单只含包管理源和 GitHub），然后结束本次运行。

`.preflight` 末尾还会写 `feedparser: OK` 或 `unavailable`。可用时优先用 feed 拿文章列表（一次请求拿到带日期的全量条目），不可用再退回读归档页。

## 1. 选题 / 编号

- **严格从 `TOPICS.md` 顶部往下取第一个未做的 slug**。清单已经按「跨模型交集 + 思想密度 + 领域轮换」排过序，且**领域已经交错编排**——照顺序写就自然不会连着三期都是技术博客，别自作主张跳序。
- 文件名 `{slug}-blog{N}.html`，**N = 写作顺序的下一个编号**（现有最大 N + 1）。
- **绝不自我生成清单**：`TOPICS.md` 由 BigCat 人工维护，routine **只读不改**（`publish.sh` 硬卡 TOPICS.md 改动）。清单全做完 → PushNotification 请 BigCat 续单、**本次不发布**、直接结束。
- **Surprise 档不许跳过**：清单里标了 `[surprise]` 的条目是专门用来打破信息茧房的。轮到它就写它，**不要因为「跟她兴趣不够贴」而跳过——那正是它存在的理由**。

## 2. 动手前：先真读

**这是本站和姊妹站最大的不同——论文是静态的、你多半训练时见过；博客是活的，你的印象很可能过时或干脆是错的。**

每期开写前必须：

1. **WebFetch 打开博客主页 + 归档页**，确认它还在，并摸清语料规模与时间跨度（多少篇、从哪年到哪年、中间有没有明显转折）。
2. **确认作者身份与基本事实**（是谁、做什么的、写了多久）。WebSearch 交叉核对。
3. **实际读 10 篇**（下限 8，读不满 8 篇就按本节末尾的规则不发布）。`TOPICS.md` 里给的 `signature_ideas` 只是线索，**不是可以直接抄的结论**。

   **怎么挑这 10 篇——按下面的级联，别随机拿最近几篇凑数：**

   | 优先 | 信号 | 怎么拿 |
   |---|---|---|
   | 1 | 作者自己的 **best-of / start here / 精选** 页 | 有就以它为准，这是作者本人说「该读什么」 |
   | 2 | **站内被回链最多的文章** | 从归档抽若干篇正文，统计站内链接指向；被后续文章反复回引的就是他的承重墙 |
   | 3 | **外部关注度** | WebSearch「{博客名} best essays / most popular」、HN / Reddit 讨论量 |
   | 4 | **评论数**（若页面可见） | 参与度代理 |

   **第 2 条优先于第 3 条**：高阅读量 ≠ 思想核心。一篇可能因标题抓人而爆红却不承载骨架；而作者十年里反复回头引用的那几篇一定是地基。本站要的是提纯一个头脑，按**思想承重**排，不按传播排。

   **时期分布是硬底线**（top-N 按关注度排会偏向"哪个时期传播得好"，可能整段错过思想成型期）：
   - **至少 2 篇来自最早期**（该博客前 20% 的时间跨度内）
   - 若该站有明显转折（改版、转型、频率突变），**转折两侧各至少 1 篇**
   - 作者若标了「被低估 / needs some love」之类的自荐冷门篇，**读 1 篇**

   **付费墙**：打不开的算没读，不计入 10 篇，换下一篇。若可读的公开文章不足 8 篇，在页面「坐标」节如实写明「本站约 N 成内容为付费订阅，本页基于公开部分」。

4. **看它最近一年还写不写**。停更的照写不误（很多经典博客已停更），但要在「坐标」里如实标出停更时间。

5. **写完前自查所读文章的发表日期**：列出这 10 篇的日期。**若全部落在 60 天内，判定不合格——回到第 3 步补读早期文章再动笔。**（2026-08-30 首期就栽在这里：读了 5 篇、全在同一个 10 天窗口内，写出来的是"这个人这个月在想什么"，不是"这个头脑是什么样"。）

**读不到就别硬写**：主页 404 / 全站失效 / 内容与清单描述严重不符 → **不发布本期**，发 PushNotification 报告该 slug 的问题请 BigCat 核实，然后结束。**绝不能靠记忆编一篇出来。**

## 3. 页面结构

写两个文件：中文 `{slug}-blog{N}.html`、英文 `{slug}-blog{N}.en.html`。

**每页两种模式，顶部 `.modebar` 药丸切换**（默认落在**精华版**，localStorage 记住选择）——沿用 `cs-papers-deepread` paper1 的 modebar CSS + 页尾内联切换脚本：

**本站是替代品，不是导读**：读完这一页，读者就**不必再去读他那几十上百篇了**。所以**把话讲完、把包袱抖光**——不许为了「勾人去读原文」而留一半。这一点和 `novel-deepread` 的全剧透同理。

但**讲完 ≠ 讲得像论文**。这是本站和 `cs-papers-deepread` 最大的分别：论文精读可以「命题 → 论证 → 结果」地摆，博客精读要**把想法讲成他发现它的过程**。姊妹站踩过的坑值得直接抄作业——novel-deepread 试过把伏笔做成「埋→收」对照表，结论是「读起来像论文附录，已废弃：这些是正文里最好看的部分，不该被搬到附录里」。**同理：论证过程是这里最好看的部分，别压成表格、别拆成带小标题的字段。**

- **短版（`.mode-pop`，默认）**：~700 字，一口气读完的**短文**，不是摘要。开头就是一件具体的怪事，结尾把他最核心那一下讲透。
- **长版（`.mode-deep`，`hidden`）**：下面七节，**按叙事推进**。

**七节（顺序即节奏，分量压在第 4 节）：**

1. **一件怪事** — 从他文章里挑**最具体、最反直觉**的东西开场：一个数字、一个空缺、一个荒唐的观察。**这是全站的腔调开关**——第一句就决定了这不是一篇人物简介。**绝不用「XX 是一位深刻的思考者」「他长期关注 YY」开场。** 判据：第一句能不能让人愣一下。
2. **这人是谁**（3–4 句）— 带**一个有画面感的细节**（读到一半退学、白天在某公司写代码、刻意把文章日期藏起来）。不写履历表、不列学位年份清单、不写「影响了一代人」这类客套。停更的在这里说明。
3. **他跟什么较劲** — 他十年没放下的那个根问题，讲**他是怎么撞上它的**，别抽象地陈述一遍。这是整页的骨架：找不到这个问题 = 没读懂，回去再读。
4. **他的想法，讲成「他怎么发现的」（主体，全篇最重）** — **2–4 个**核心想法，每个按**发现的顺序**讲，不按论文的顺序讲：他注意到什么怪事 → 主流怎么解释、他为什么觉得不对 → **他去做了什么**（去数什么、举什么例子、拿什么当撬棍）→ 结论。
   - **是叙述不是罗列。**别给每个想法套「命题 / 论证 / 反直觉 / 出处」四个小标题——那正是论文腔。让因果自己在行文里走。
   - **每个至少配一句他的原话。**原话在这里是**乐趣本身**，不只是证据——挑他写得最好、最狠、最像他的那句。
   - **讲透，别留白。**读者读完这一节就该能把他的论证复述给别人听。
   - 链到原文（出处，不是「去读它」的邀请）。
5. **他的思维方法** — 读长期写作者最可迁移的往往不是结论而是方法：怎么选题、怎么找证据、怎么对待反例、怎么处理不确定。**必须从你实际读过的文章里归纳**，别泛泛说「他很严谨」。这一节最容易出金句，可以写得俏皮。
6. **他哪里可能是错的** — 诚实写盲区、被有力反驳之处、立场带来的系统性偏斜。**不吹捧。**有实名的、有分量的批评者就点出来；**读不到批评原文就只报书目事实、明说没读过，绝不复述没读过的论证。**若他自己写下过最狠的那条批评，那是最好的写法。
7. **出处** — 3–5 篇代表作，标题 + 链接 + 一句它讲什么。**这是出处不是导读**：别写「读它是为了什么」「从哪篇开始」——那是阅读指南，本站不写。链接必须验证能打开。

**不写「谁该读」「怎么读」「读完读什么」「这对你的启发」这类填充节。**

## 4. 更新记录（月更 routine 写，日更 routine 只留位置）

每页在「必读清单」之后、页尾之前，留一个空的更新区：

```html
<section class="updates" data-slug="{slug}" data-last-reviewed="{写作当天 YYYY-MM-DD}">
  <h2>更新记录</h2>
  <!-- updates -->
</section>
```

日更 routine **只负责留下这个空壳**（带正确的 `data-slug` 和 `data-last-reviewed`），不写任何内容。月更 routine 会往 `<!-- updates -->` 前插条目。英文页同样留一个 `<h2>Updates</h2>` 的空壳。

## 5. 写作要求

- **篇幅：中文全页目标 3200–3600 字**（精华版 ~800 + 深读版 ~2600）。`.maxchars` 是 4000，那是**硬兜底、不是目标**——正常成稿不该接近它。

  **动笔前先分配预算**，别写完再削：精华版 ~800；深读版七节里，第 4 节「核心思想」占 ~1200（主体），第 3 节「他在跟什么较劲」~350，第 5 节「思维方法」~400，其余四节合计 ~650。按这个额度写，落点自然在 3400 左右。

  **万一超了，删掉一整个例子、一整段、或一个次要命题——绝不许靠删「的 / 了 / 个」、把词换成更短的同义词来凑。** 那种微雕削掉的多是连接词，句子会变生硬，而且要削十几轮才够。2026-08-30 首期实测：先写到 4432 字，然后做了 19 轮微雕才压到 3997，每轮只削 2–9 个字——纯属浪费，且成稿的中文比修剪前更硬。**一刀砍掉一个例子，一次就够了，读起来还更好。**
- **面向零基础读者**：假设读者不认识这个人、不熟他所在的领域。「一句话」之后如果本篇会用到该领域的前置概念，加一个 `<h2>Glossary · 术语表</h2>` 小节（`.glossary` 虚线框裹 `<ul>`），每个前置词一句大白话。正文其余术语随名即释。
- **必须引用他自己的原话**：每个核心命题至少配**一句**他的原文引语（英文博客给英文原句 + 中译；中文博客直接给原句），并注明出自哪篇。**引语必须是你 WebFetch 实际读到的，一个字都不许编。**这是本站的生命线——编造某人说过的话，比编造论文数字更恶劣。
- **拿不准就不写**：记不清、找不到出处、只有模糊印象的，一律删掉，不要用「他大意是说」蒙混。
- **不神化也不看轻**：这些人多数有强烈的个人风格和明确的立场。如实呈现，包括你不同意的部分。**不要因为某人立场与主流不同就加缓冲垫或免责声明**——第 6 节自会处理争议。
- **配图按需**：只有当涉及**有空间结构的东西**（他提出的某个模型、框架、流程、分类学）时才配内联 SVG 自绘图，配 `figcaption`。**纯思想论述不要硬配图。**
- **中英双语，两版都地道**：split 模式，两个独立文件、langbar 互链。**英文页除 langbar 的「中文 →」外不得出现汉字**（含 SVG 标注）。**例外**：写中文博客时，作者姓名、博客名、被引原文可保留中文，但每处必须挂罗马转写 + 英文释义。
- **中文页术语首次出现补英文**：`中文术语（English）`。人名、博客名照原样不译。
- **腔调（本站的性格，别写成论文精读）**：
  - **可以有文学性、有节奏、有幽默**；短句、直接对读者说话、偶尔一句俏皮话都行。
  - 但**不抒情过度、不替读者感动**，全篇不出现「震撼人心」「不可不读」「令人拍案叫绝」这类推荐语——你在提纯一个头脑，不在写腰封。
  - **具体压过抽象**：能给一个他举过的真例子，就别写一句概括。读者记得住的永远是床单为什么没有品牌广告，不是「他擅长用反例论证」。
  - **别怕说人话**：术语能换成大白话就换，换不了就当场一句话解释。
- **重点加粗**，克制。

## 6. 版式

复用 `cs-papers-deepread` paper1 的内联 `<style>` 结构（无衬线、渐变标题、mono 小标签、玻璃拟态卡片、modebar），但**配色改成本站专属的靛蓝／青瓷**：主色 `#6ea8fe` → `#5eead4` 渐变，冷暗底 `#0d1117`。**别用 cs-papers 的琥珀铜色**（那是姊妹站的）。

## 7. 文件约定 / 发布

- 文件放仓库根目录：`{slug}-blog{N}.html` + `{slug}-blog{N}.en.html`。
- 发布前更新 `index.html`（按 slug 找到该博客的灰色占位行，**原地**改成链接、去掉 `todo` 类；**绝不在末尾 append**）+ `index.en.html` 同样处理。灰行 → 链接的完整形状：

  ```html
  <!-- 之前 -->
  <div class="entry todo"><span class="date">—</span><span class="title">作者 — 博客名</span><span class="meta">example.com</span></div>
  <!-- 之后 -->
  <a class="entry" href="{slug}-blog{N}.html" data-updated="2026-08-30"><span class="date">2026-08-30</span><span class="title">作者 — 一句钩子</span><span class="meta">example.com</span></a>
  ```

  **`data-updated` 和 `.date` 两处都要填今天的日期，且必须与页内 `<section class="updates" data-last-reviewed="…">` 完全一致**（三处同一个日期）。这个日期是「本页内容核对到哪一天为止」，不是发表日——月更回访会把它往前推。
- **别在 index 里加 ★ 或任何 surprise 标记**：清单里的 `[surprise]` 只是给选题用的，读者侧不区分。
- **每次运行开头先「对齐」灰行**：扫 `TOPICS.md`，凡是 slug 没有对应页、且 index 里没有灰行的，按清单顺序补一条 `<div class="entry todo">`（`.date` 填 `—`、不带 `data-updated`），插在 `<div class="group" data-todo>` 之后。两个 index 都要补。
- **不用管排序**：index 页尾有一段内联脚本，会按 `data-updated` 把已发布条目倒序排到最前——你只管把日期填对，顺序它自己会排。
- **index 条目的 `.title` 是钩子不是摘要**：`作者名 / 博客名 — 一个 clause 点出他最核心的那一个洞察`，中文 ≤ 约 50 字。
- **不要**手动加 `comments.js` / `search.js` / `index-button.js` / `i18n-tts.js` / `lightbox.js`（GitHub Action 自动注入），也别硬写 `← Hub`。
- 用 `./publish.sh` 发布。git：`user.name=BigCat` / `user.email=chengchen0802@gmail.com`。

## 8. 完成后

调用 **PushNotification**（`status:"proactive"`，一行 < 200 字、无 markdown）：
`博客精读已更新：Gwern · 用「长期主义写作」对抗互联网的健忘 · cissy0802.github.io/blog-deepread`
