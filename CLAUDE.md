# CLAUDE.md — 博客精读 · blog-deepread

这是云端 routine 的执行说明。你是引擎本身（Opus），**自主完成、不等任何人确认**。当前工作目录已挂载本仓库。

定位：每期挑**一个**博客／写作者，把这个头脑**最精华的思想**提炼出来、讲透。目标是：**读完这一页，约等于把这个人读通了——他在想什么问题、给出了哪几个真正原创的答案、他怎么想问题、他哪里可能是错的——读者不必自己去爬完他十年几百篇文章。**

**这不是「博客简介」，不是「文章列表」，不是「安利」。是提纯一个头脑。**

一个长期写作者的价值，往往集中在两三个他反复回来、越挖越深的核心命题上，其余是这些命题在不同题材上的应用。你的活就是把那两三个命题拎出来、讲到读者能复述、能拿去用、能判断自己同不同意。

---

## 0. 选题 / 编号

- **严格从 `TOPICS.md` 顶部往下取第一个未做的 slug**。清单已经按「跨模型交集 + 思想密度 + 领域轮换」排过序，且**领域已经交错编排**——照顺序写就自然不会连着三期都是技术博客，别自作主张跳序。
- 文件名 `{slug}-blog{N}.html`，**N = 写作顺序的下一个编号**（现有最大 N + 1）。
- **绝不自我生成清单**：`TOPICS.md` 由 BigCat 人工维护，routine **只读不改**（`publish.sh` 硬卡 TOPICS.md 改动）。清单全做完 → PushNotification 请 BigCat 续单、**本次不发布**、直接结束。
- **Surprise 档不许跳过**：清单里标了 `[surprise]` 的条目是专门用来打破信息茧房的。轮到它就写它，**不要因为「跟她兴趣不够贴」而跳过——那正是它存在的理由**。

## 1. 动手前：先真读

**这是本站和姊妹站最大的不同——论文是静态的、你多半训练时见过；博客是活的，你的印象很可能过时或干脆是错的。**

每期开写前必须：

1. **WebFetch 打开博客主页**，确认它还在、看清它现在长什么样。
2. **实际读若干篇代表作**（至少 4–6 篇，覆盖不同时期）。`TOPICS.md` 里给的 `signature_ideas` 只是线索，**不是可以直接抄的结论**——你要自己读过再下判断。
3. **确认作者身份与基本事实**（是谁、做什么的、写了多久）。WebSearch 交叉核对。
4. **看它最近一年还写不写**。停更的照写不误（很多经典博客已停更），但要在「坐标」里如实标出停更时间。

**读不到就别硬写**：主页 404 / 全站失效 / 内容与清单描述严重不符 → **不发布本期**，发 PushNotification 报告该 slug 的问题请 BigCat 核实，然后结束。**绝不能靠记忆编一篇出来。**

## 2. 页面结构

写两个文件：中文 `{slug}-blog{N}.html`、英文 `{slug}-blog{N}.en.html`。

**每页两种模式，顶部 `.modebar` 药丸切换**（默认落在**精华版**，localStorage 记住选择）——沿用 `cs-papers-deepread` paper1 的 modebar CSS + 页尾内联切换脚本：

- **精华版（`.mode-pop`，默认）**：~700–1000 字。给「我只想知道这人到底牛在哪」的读者。结构：他在跟什么较劲 → 他的答案（2–3 个核心命题，每个一段大白话）→ 一个最能代表他的具体例子 → 读他的正确姿势（从哪篇入门）。**零术语堆砌，每个命题都要过「饭桌复述测试」。**
- **深读版（`.mode-deep`，`hidden`）**：下面**七节**全展开。

七节：

1. **一句话** — 这个人到底在想什么。要能立住，别写「一位深刻的思考者」这种废话。
2. **坐标** — 谁（真名、身份、职业背景）+ 写了多久（起始年份→至今／停更于某年）+ 什么形态（长文随笔／技术深挖／札记）+ 大致产量。一两句，别写传记。**停更的必须在这里标明。**
3. **他在跟什么较劲** — 这个人反复回来的那个根问题是什么。这是整页的骨架：一个长期写作者之所以值得读，是因为他有一个真问题，且十年没放下。找不到这个问题 = 你还没读懂他，回去再读。
4. **核心思想，讲透（主体，分量压这里）** — **2–4 个**他真正原创的命题。每个：
   - **命题是什么**（一句话立论）
   - **他怎么论证的**（他的路径、他举的证据／例子）
   - **为什么反直觉 / 为什么不是常识**（如果这条是常识，它就不该进这一节）
   - **出处**：至少链到一篇他自己的原文
5. **他的思维方法** — **这是博客区别于论文的地方**：读一个长期写作者，最可迁移的往往不是结论而是方法。他怎么选题、怎么找证据、怎么对待反例、怎么处理不确定、文章怎么组织。**必须从你实际读过的文章里归纳，不能泛泛说「他很严谨」。**
6. **局限与争议** — 诚实写：他的盲区、被有力反驳过的地方、他的立场带来的系统性偏斜、哪类问题他会失手。**不吹捧。**如果有实名的、有分量的批评者，点出来。
7. **必读清单** — 3–5 篇代表作。每篇：标题 + 真实链接 + 一句话说清它讲什么、为什么该读它。**链接必须 WebFetch 验证过能打开。**

**不写「谁该读」「读完读什么」「这对你的启发」这类填充节。**

## 3. 更新记录（月更 routine 写，日更 routine 只留位置）

每页在「必读清单」之后、页尾之前，留一个空的更新区：

```html
<section class="updates" data-slug="{slug}" data-last-reviewed="{写作当天 YYYY-MM-DD}">
  <h2>更新记录</h2>
  <!-- updates -->
</section>
```

日更 routine **只负责留下这个空壳**（带正确的 `data-slug` 和 `data-last-reviewed`），不写任何内容。月更 routine 会往 `<!-- updates -->` 前插条目。英文页同样留一个 `<h2>Updates</h2>` 的空壳。

## 4. 写作要求

- **篇幅：中文全页 2500–4000 字**（精华版 + 深读版合计），宁精炼勿注水。仓库根目录有 `.maxchars`，超了 `publish.sh` 会弹回。
- **面向零基础读者**：假设读者不认识这个人、不熟他所在的领域。「一句话」之后如果本篇会用到该领域的前置概念，加一个 `<h2>Glossary · 术语表</h2>` 小节（`.glossary` 虚线框裹 `<ul>`），每个前置词一句大白话。正文其余术语随名即释。
- **必须引用他自己的原话**：每个核心命题至少配**一句**他的原文引语（英文博客给英文原句 + 中译；中文博客直接给原句），并注明出自哪篇。**引语必须是你 WebFetch 实际读到的，一个字都不许编。**这是本站的生命线——编造某人说过的话，比编造论文数字更恶劣。
- **拿不准就不写**：记不清、找不到出处、只有模糊印象的，一律删掉，不要用「他大意是说」蒙混。
- **不神化也不看轻**：这些人多数有强烈的个人风格和明确的立场。如实呈现，包括你不同意的部分。**不要因为某人立场与主流不同就加缓冲垫或免责声明**——第 6 节自会处理争议。
- **配图按需**：只有当涉及**有空间结构的东西**（他提出的某个模型、框架、流程、分类学）时才配内联 SVG 自绘图，配 `figcaption`。**纯思想论述不要硬配图。**
- **中英双语，两版都地道**：split 模式，两个独立文件、langbar 互链。**英文页除 langbar 的「中文 →」外不得出现汉字**（含 SVG 标注）。**例外**：写中文博客时，作者姓名、博客名、被引原文可保留中文，但每处必须挂罗马转写 + 英文释义。
- **中文页术语首次出现补英文**：`中文术语（English）`。人名、博客名照原样不译。
- **重点加粗**，克制。

## 5. 版式

复用 `cs-papers-deepread` paper1 的内联 `<style>` 结构（无衬线、渐变标题、mono 小标签、玻璃拟态卡片、modebar），但**配色改成本站专属的靛蓝／青瓷**：主色 `#6ea8fe` → `#5eead4` 渐变，冷暗底 `#0d1117`。**别用 cs-papers 的琥珀铜色**（那是姊妹站的）。

## 6. 文件约定 / 发布

- 文件放仓库根目录：`{slug}-blog{N}.html` + `{slug}-blog{N}.en.html`。
- 发布前更新 `index.html`（按 slug 找到该博客的灰色占位行，**原地**改成 `<a class="entry" href="…">`、去掉 `todo` 类、加 href；**绝不在末尾 append**）+ `index.en.html` 同样处理。
- **每次运行开头先「对齐」灰行**：扫 `TOPICS.md`，凡是 slug 没有对应页、且 index 里没有灰行的，按清单顺序补一条 `<div class="entry todo">`。两个 index 都要补。
- **index 条目的 `.title` 是钩子不是摘要**：`作者名 / 博客名 — 一个 clause 点出他最核心的那一个洞察`，中文 ≤ 约 50 字。
- **不要**手动加 `comments.js` / `search.js` / `index-button.js` / `i18n-tts.js` / `lightbox.js`（GitHub Action 自动注入），也别硬写 `← Hub`。
- 用 `./publish.sh` 发布。git：`user.name=BigCat` / `user.email=chengchen0802@gmail.com`。

## 7. 完成后

调用 **PushNotification**（`status:"proactive"`，一行 < 200 字、无 markdown）：
`博客精读已更新：Gwern · 用「长期主义写作」对抗互联网的健忘 · cissy0802.github.io/blog-deepread`
