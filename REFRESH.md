# REFRESH.md — 博客精读 · 月度回访引擎

这是**月更 routine** 的执行说明（日更引擎见 `CLAUDE.md`，两者互不干扰）。你是引擎本身（Opus），**自主完成、不等任何人确认**。

## 这个 routine 解决什么问题

博客和论文最大的不同：**论文写完就定了，博客的作者还活着、还在写、还会推翻自己。**

日更 routine 写下的那一页，是「截至写作那天，这个人是这么想的」。一年后它可能已经过时——作者可能深化了某个命题、公开改了主意、卷入了一场重要争论、或者干脆停更了。

本 routine 每月跑一次，**把已发布的页面重新对齐到作者的当前状态**，让每一页始终是活的。

## 硬边界（先说不许干什么）

- **绝不改 `TOPICS.md`**（`publish.sh` 硬卡）。
- **绝不改已有正文的七节内容**。原文是当时的忠实记录，**不许回填、不许润色、不许"修正"**。新东西一律进「更新记录」区。唯一例外见下面「停更 / 失效」。
- **绝不写没读过的东西**。没 WebFetch 打开过的文章，不许进更新记录。
- **没有新东西就别硬写**。一个季度不发文的博客很正常，此时只更新 `data-last-reviewed` 日期，不加任何条目。**宁可整轮零更新，也不要为了显得干了活而写"他继续保持了一贯的深刻"这种废话。**

## 执行步骤

### 1. 盘点
`ls *-blog*.html | grep -v '\.en\.html'` 得到所有已发布页。对每一页读出 `<section class="updates">` 上的 `data-slug` 和 `data-last-reviewed`。

按 `data-last-reviewed` **从早到晚排序**——最久没回访的优先。

### 2. 探查（对排序后的每一页，全部都探）
- WebFetch 博客主页 / 归档页 / RSS，找出 `data-last-reviewed` 之后发表的文章。
- 记下：有几篇新文、标题是什么、大致在讲什么。
- **主页打不开**：再试一次（换 `/archive`、`/posts`、RSS）。仍失败 → 记为「疑似失效」，进第 4 步的报告，**本轮不改该页**。

### 3. 判断哪些值得写（这是本 routine 的核心判断）

**只有满足下列任一条，才值得进更新记录：**

- **深化**：他把原页第 4 节某个核心命题往前推了一步（新论证、新证据、新的适用边界）。
- **自我推翻**：他公开改了主意，或承认原来某个判断错了。**这是最高价值的一类，优先写。**
- **新命题**：他开始较劲一个原页没有的新问题，且已经写了不止一篇。
- **重要争论**：他跟某个有分量的人公开交锋，且交锋本身暴露了他思想的边界。
- **停更**：确认他已经不写了（超过 12 个月无新文）。

**明确不值得写的：**
- 单篇好文但没推进任何核心命题（那只是"他又写了一篇好文章"）
- 转载、公告、书讯、播客链接、生活流水
- 只是把老观点换个题材再说一遍
- 你无法判断"这算不算推进"的——**存疑就不写**

**每轮至多深写 8 页。** 若合格的多于 8 个，取「自我推翻 > 深化 > 新命题 > 重要争论」的优先级，超出的留到下轮（它们的 `last_reviewed` 不动，下轮自然排前面）。

### 4. 写入

对每个要更新的页面，在 `<!-- updates -->` **之前**插入一条：

```html
<div class="update-item">
  <div class="update-date">2026-09</div>
  <div class="update-body">
    <strong>[深化 / 自我推翻 / 新命题 / 争论]</strong> 一到两段，说清：<b>新东西是什么、它跟原页第几节的哪个命题什么关系、这改变了什么</b>。
    必须至少链一篇原文：<a href="…">文章标题</a>。若是自我推翻，把「他原先怎么说 → 现在怎么说 → 他给的理由」三段都点到。
  </div>
</div>
```

然后把该 `<section class="updates">` 的 `data-last-reviewed` 改成**今天**。

**英文页同步**：同一条目译成地道英文插进 `.en.html` 的对应位置，日期与 `data-last-reviewed` 一致。**英文页不得出现汉字**（中文博客的原文引语除外，需挂转写 + 英译）。

**没有合格更新的页面**：只把 `data-last-reviewed` 改成今天，不插条目。

**停更确认**：这是唯一允许动正文的情况——把第 2 节「坐标」里的活跃状态改成「停更于 {年份}」，同时在更新记录里写一条说明。

### 5. index 与发布

**改过的页面必须同步改 index 的日期**（这是本 routine 唯一要动 index 的地方，别漏）：在 `index.html` 和 `index.en.html` 里按 `href="{slug}-blog{N}.html"` 找到该条目，把 `data-updated` 属性和 `<span class="date">` 的文字**都**改成今天，与页内 `data-last-reviewed` 保持三处一致。

这一步不是装饰，它驱动两件事：
- index 页尾的排序脚本按 `data-updated` 倒序排，刚回访过的条目会自动浮到最前；
- `reads.js` 拿这个日期和「读者标记已读的时间戳」比对——日期更新则该条目的 ✓ 变成橙色 ↻「读后有更新」。**不改 index 日期，读者就不会知道这页变了**，整个回访白做。

`data-last-reviewed` 只改了、index 没跟上（或反过来）是最容易出的错，写完自查一遍三处日期是否一致。

- 不新增 index 条目（更新不产生新条目），只改日期。
- 跑 `./publish.sh`。它会把「修改过的 `*-blog{N}.html`」识别为待发布文件，索引引用检查会通过（这些页早就在 index 里）。
- 若本轮**一页都没改**（所有博客都没新东西）：**不要 commit**，发一条 PushNotification 说明「本月 N 个博客均无实质更新」，结束。

### 6. 完成后

PushNotification（`status:"proactive"`，一行 < 200 字、无 markdown），说清**改了几页、最值得看的那一条是什么**：

`博客精读月度回访：更新 5 页 · Gwern 公开修正了他对 scaling 的判断 · cissy0802.github.io/blog-deepread`

若有疑似失效的博客，在推送里点名，让 BigCat 去核实是否要从清单里撤下。
